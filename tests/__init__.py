"""The test suite.

Everything runs with ``python -m unittest discover`` and with ``pytest``; no
test depends on which runner drives it, and none touches the network, the wall
clock, or anything outside a temporary directory it created itself.
"""
